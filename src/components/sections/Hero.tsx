import { FC } from 'react';


interface HeroProps {}

const Hero: FC<HeroProps> = ({}) => {
    return (
        <section className="flex mt-10">
           
        </section>
    );
};

export default Hero;
